# 1万日散歩するやつ — アプリ仕様書

本人だけが使う個人用iPhoneアプリ。毎朝開いて「今日歩いた」を押し、そのまま「Xで投稿」で画像付き投稿まで完結させる。
GitHub Pagesはアプリの置き場所として使うだけ（URLは本人のみ利用、記録はiPhone内に保存）。Safariの「ホーム画面に追加」でアプリとして起動する。

## 技術方針

- 単一の `index.html`（CSS/JSを内包）。ビルド不要。
- データは `localStorage` に保存。外部サーバー・ログインなし。
- `manifest.json` を同梱し、ホーム画面追加時にスタンドアロン表示・アイコン付きにする。
- フォントは Google Fonts の **DotGothic16**（日本語ドット文字）。
- 画像生成は `<canvas>` で 1080×1080 を描画し、`navigator.share({ files })` で写真アプリに保存。非対応時は `<a download>` にフォールバック。
- リポジトリ作成 → push → GitHub Pages 有効化まで行う。

## データモデル

```json
{
  "startDate": "2026-09-01",
  "walkedDates": ["2026-09-01", "2026-09-02", "..."]
}
```

- **カウントは「歩いた日の数」**（`walkedDates.length`）。カレンダー日数ではない。休んだ日は進まない。
- `startDate` は表示用のみ。計算には使わない。
- 日付は端末のローカル時刻で `YYYY-MM-DD`。重複登録は不可。

### 導出値

| 名前 | 計算 |
|---|---|
| 日数 `n` | `walkedDates.length` |
| カード番号 | `floor((n-1)/100) + 1`（n=0のとき1） |
| カード内の塗り数 | `n - (カード番号-1)*100` |
| 連続 | 今日または昨日を終点とする連続日数。どちらも未登録なら0 |
| 最長連続 | 全期間の最長ラン |

## 画面（アプリ本体）

縦1画面。上から:

1. 投稿画像のプレビュー（canvasをそのまま表示、正方形、横幅いっぱい）
2. 主ボタン **「今日歩いた」**
   - 今日が登録済みなら「今日は記録済み」表示にして無効化
3. 副ボタン **「昨日の分を記録」**（押し忘れ救済。昨日が登録済みなら非表示）
4. 主ボタン **「Xで投稿」**
   - `navigator.share({ files: [png] })` でiPhoneの共有シートを開く。ユーザーがXを選ぶと画像付きの投稿画面が開く
   - 共有テキストとして `{n}日目 / 10000` を渡す（Xが受け取れば本文に入る）
   - 共有非対応の環境では「画像を保存」（`<a download>`）にフォールバック
5. 小さく「設定」リンク

### 設定画面

- 開始日の変更
- 初期セットアップ用: 「ここまで歩いた回数」を入力すると、開始日から連続でその日数分を登録する（既存データがあるときは確認ダイアログ）
- データのエクスポート（JSONをテキスト表示、コピーボタン）
- データのインポート（JSONを貼り付け）
- 全データ削除（確認ダイアログ2回）

## 投稿画像の仕様（最重要）

正方形 1080×1080。背景 `#000`。外枠 6px `#39FF14`、角丸 20px。
フォント DotGothic16。色は2色のみ:

- 明るい緑 `#39FF14` … 日数・塗られたマス・連続/最長・「N枚目 = 100日」
- 暗い緑 `#1E7A0F` … `/ 10000`・日付・「全100枚 = 1万日」・拡大線
- 未塗りマス枠線 `#145A0A`、小格子の未塗り `#0B2A05`

### レイアウト（余白 60px、左揃え）

```
┌──────────────────────────────────────┐
│ 1万日散歩するやつ                    │  34px 明緑
│                                      │
│ 6日目 / 10000                        │  数字165px 明緑 / 「日目」50px 明緑 / 「/ 10000」50px 暗緑
│ 2026.09.01 → 2026.09.06              │  34px 暗緑（開始日 → 今日）
│                                      │
│ [小格子]──拡大線──[大格子 10×10]     │
│ 全100枚 = 1万日      1枚目 = 100日    │  34px（左:暗緑 右:明緑）
│ ──────────────────────────────────── │  区切り線 2px #145A0A
│ 連続 6日                  最長 6日    │  38px 明緑
└──────────────────────────────────────┘
```

### 格子

- **大格子**: 10×10、セル44px、間隔12px。左上から右へ、行ごとに塗る。塗り=`#39FF14`塗りつぶし、未塗り=`#145A0A` 3px枠線。
- **小格子**: 10×10、セル18px、間隔6px。現在のカードのセルだけ `#39FF14` で塗り、周囲に2pxの明緑の枠を1px離して描く。過去のカードは `#39FF14` で塗る（枠なし）。未来は `#0B2A05`。
- **拡大線**: 小格子の右端から大格子の左端へ2本の線（2px `#1E7A0F`）。上の線は「現在のカードのセルの上辺」から大格子の上辺へ、下の線は「同セルの下辺」から大格子の下辺へ。地図の拡大図と同じ表現。
- 横幅の配分: 小格子 234px、拡大線エリア 130px、大格子 548px。3つは上端揃え。

### 数値表記

- 日数は3桁区切りなし（`1234日目`）
- 日付は `YYYY.MM.DD`

## PWA

- `manifest.json`: name「1万日散歩するやつ」、`display: standalone`、`background_color: #000`、`theme_color: #000`
- アイコン: 黒背景に明緑の10×10格子（左上1マスだけ塗り）。512px と 180px（apple-touch-icon）
- `<meta name="apple-mobile-web-app-capable" content="yes">` と `apple-mobile-web-app-status-bar-style: black`

## やらないこと（今回の範囲外）

- 写真の合成
- ひとこと入力
- 休んだ日数の表示
- 通知
- 複数端末同期

## 完了条件

1. GitHub Pages のURLをiPhone Safariで開き、「ホーム画面に追加」で起動できる
2. 「今日歩いた」で大格子が1マス増え、連続が更新される
3. 「Xで投稿」でiPhoneの共有シートが開き、Xを選ぶと1080×1080の画像付きで投稿画面が開く
4. アプリを閉じて開き直しても記録が残っている
